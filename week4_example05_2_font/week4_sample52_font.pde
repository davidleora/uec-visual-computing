size(600, 100);
background(255);
textFont(createFont("Tempus Sans ITC", 24));
fill(0);
textAlign(CENTER);
text("Welcome to Visual Computing Class!!", width / 2, height / 2);

// text syntax
// text(data, x, y); OR text(data, x, y, z); OR text(data, x, y, width, height);
// data: string of text you want to display
// x, y: coordinates of the text's starting point
// z: in 3D sketches, this specifies te depth position of the text
// width, height: optional parameters define a box within which the text will wrap


// textFont syntax
// textFont(font); OR textFont(font, size);
